{
  "kit": "AI-DEV-STARTER",
  "url": "https://github.com/HarryPehkonen/AI-DEV-STARTER",
  "revision": "fb387f2044989f76bb2fe234cef39e85ac050328",
  "recorded_at": "2026-09-20",
  "record_kind": "retroactive",
  "record_note": "Wired by hand on 2026-09-18 (commit 3ee8251, 'ci: local gate runner + git hooks (no GitHub); format drift fixed; tidy clean'), before the kit existed as a git repo — so `revision` is the kit's FIRST PUBLISHED commit, not the exact copy point; do not read it as the tree this repo was copied from. The direction here is the reverse of the other recorded repos': JSOM's gate is one of the three real instances the kit's templates were generalised FROM (DESIGN-NOTES.md section 1 — the 2026-09-19 MoA design pass carried 'JSOM's tools/ci.sh + both hooks + .ci.env.example' verbatim in its prompt), and the only kit-to-repo traffic since is the tidy-baseline fix ported in 67302cc (card t_1eab5d69, already applied to all four files below). Two artifacts in the convention's C++ set have no entry because this repo never took them: INCIDENTS.md was never copied (grep -rn INCIDENTS over the repo returns nothing) and there is no kit-derived agent contract — CLAUDE.md is this repo's own, predating the kit (2026-02-15, extended 2026-09-08 and 2026-09-19) and following a different design from templates/CLAUDE.md.template, so a sync must never overwrite it. Synced on 2026-09-20 by card t_d899b8ff (the commit that carries this hash update): the four gate-contract rules this script used to predate — a final GATE PASSED/GATE FAILED token (it used to print only 'all N stage(s) passed in Xs'), BLOCK lines for every requested stage that did not run, export NO_COLOR=1 (this script greps tool output, and ANSI escapes defeat the greps), and a derived rather than hard-coded usage header range (was sed -n '2,21p') — plus the end-of-run summary the kit and jsonTools both print. Both hooks gained the kit's provenance line, with their deviation stated there instead of pointing at an INCIDENTS.md this repo does not have. Rule that keeps this file true: any commit that edits a copied artifact must update that artifact's repo_sha256 in the same commit.",
  "files": [
    {
      "repo_path": "tools/ci.sh",
      "kit_path": "templates/cpp/ci.sh",
      "repo_sha256": "8132e0b4f6f0ffe49f63cd4ceb1af6a43805f5285bc254a7a6d3e8adee5e2ba3",
      "kit_sha256": "dcccb2066b856ec4bbbe04e98d74c37fffe5372a1486cab47623f403b9f613d1",
      "adapted": true,
      "adapted_why": "This file is the kit template's ancestor, not an instance of it (525 lines vs 634), and it stays specialised on purpose: only CI_JOBS/CI_BUILD_DIR/CI_ASAN_BUILD_DIR/CI_FUZZ_SECONDS/CI_LOG_DIR/CI_STRICT_TOOLS/CI_KEEP_TMP/CI_DEFAULT_STAGES/CI_TIDY_BASELINE are knobs, the build dirs and the test command are hard-coded, there is no version stage, and it adds four stages the template does not have (fuzz, std, cli, conform) plus opt-in coverage — a sync must not force the template's parameterisation or stage set onto it. The template's tidy-baseline fix (tidy_key applied to both operands, plus --write-tidy-baseline) is already here via 67302cc, so that divergence is not lag; the four gate-contract rules it used to lack (verdict token, BLOCK lines, NO_COLOR, derived usage range) came in with t_d899b8ff on 2026-09-20, so every divergence left is deliberate."
    },
    {
      "repo_path": ".ci.env.example",
      "kit_path": "templates/cpp/.ci.env.example",
      "repo_sha256": "b522a56830e4b32c3fd9964f691449a7f57954d896cc6b7d98c8fd7d3392baac",
      "kit_sha256": "777a750755a10d79674d2ff7605130ceed14668416b44c3acefd73d819e43308",
      "adapted": true,
      "adapted_why": "Documents this script's knobs only: the template's CI_BUILD_TYPE, CI_SOURCE_GLOBS, CI_CMAKE_FLAGS, CI_TEST_CMD, CI_ASAN_TEST_CMD, CI_VERSION_HEADER and CI_VERSION_BINARIES blocks have no counterpart because tools/ci.sh hard-codes those, and this file adds CI_FUZZ_SECONDS, which the template does not have. Its clang-tidy section is the template's post-fix wording (both operands normalised, capture through the gate, never by hand), i.e. in sync thanks to 67302cc."
    },
    {
      "repo_path": ".githooks/pre-commit",
      "kit_path": "templates/hooks/pre-commit",
      "repo_sha256": "88c8bc3ed650a79b3a6d3bc43002f5ce673a5a7fde994885f84c1ec9efbf98e8",
      "kit_sha256": "e304ddcb48e216029c8e77402d927695dae4895532f6bf12d1ecb54604b652d8",
      "adapted": true,
      "adapted_why": "C++-only, and still five code lines: the deno/python branch and the 'no gate found' message are deleted (a missing tools/ci.sh still fails the commit closed — exec returns 127). Same two-stage fast tier as the template (build tests). Since t_d899b8ff (2026-09-20) the header carries the kit's provenance line, with that deviation stated there instead of pointing at an INCIDENTS.md this repo does not have."
    },
    {
      "repo_path": ".githooks/pre-push",
      "kit_path": "templates/hooks/pre-push",
      "repo_sha256": "2b542c7b984cba1f8f7ed49168a62998ff257fe9e54d58b0f738bc3620f457ee",
      "kit_sha256": "2c82ed7099fc5687795264521be327fdda23e8c02eadd3ac12bd9667186c0d18",
      "adapted": true,
      "adapted_why": "Passes this repo's own stage list (tree format build tests asan fuzz tsan std cli conform tidy pristine) instead of the template's eight, drops the deno/python branch, and adds a tag-only-push fast path the template does not have (4748168): a tag whose commit is already on a remote branch skips the gates, while a tag pointing at a local-only commit still runs them. Stage list unchanged by t_d899b8ff (2026-09-20), which only added the kit's provenance line to the header, stating this deviation there instead of pointing at an INCIDENTS.md this repo does not have."
    }
  ]
}
